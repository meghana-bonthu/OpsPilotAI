services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    environment:
      ACCEPT_EULA: "Y"
      MSSQL_SA_PASSWORD: "${OPSPILOT_SQL_PASSWORD}"
    ports:
      - "1433:1433"
    volumes:
      - opspilot-sql:/var/opt/mssql

volumes:
  opspilot-sql:
